version: '3.8'

services:
  gateway:
    image: llm-agent-gateway:${GATEWAY_VERSION:-latest}
    ports:
      - "8000:8000"
    env_file:
      - .env
    networks:
      - network_public # Use the external network  

networks:
  network_public:
    external: true
    name: network_public